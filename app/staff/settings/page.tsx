import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function StaffSettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Config"
      title="Settings"
      phase="Phase 13 · Polish + Testing"
      description="Threshold defaults, expiration warning days, and admin tools live here."
    />
  );
}
