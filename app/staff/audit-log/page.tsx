import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function StaffAuditLogPage() {
  return (
    <PlaceholderPage
      eyebrow="Trace"
      title="Audit log"
      phase="Phase 11 · Audit Log UI"
      description="Every state-changing staff action is recorded. Filter by actor, date, action type."
    />
  );
}
