import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function StaffStudentsPage() {
  return (
    <PlaceholderPage
      eyebrow="Roster"
      title="Students"
      phase="Phase 10 · Reports + Dashboard"
      description="Student directory and per-student borrow history live here."
    />
  );
}
