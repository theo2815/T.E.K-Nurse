import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function StaffScanPage() {
  return (
    <PlaceholderPage
      eyebrow="CAMERA"
      title="Scan"
      phase="Phase 6 · Scan + QR"
      description="QR scanner for SKU drawer cards. Walk-in borrow and consumable usage flows start here."
    />
  );
}
